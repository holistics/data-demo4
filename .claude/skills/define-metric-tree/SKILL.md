---
name: define-metric-tree
description: Turn raw interview output into a governed metric tree — a north-star metric decomposed into MECE drivers and guardrails, with a full specification per metric (grain, numerator, denominator, inclusions, exclusions, default cut, aliases, owner) ready to encode as dataset-level metrics in Holistics. Use as Phase 4 of new-analytics-onboarding, or standalone when a customer needs a metric framework, a driver tree, a metrics glossary, or help deciding which metrics to define first. Also trigger on 'metric tree', 'driver tree', 'north star metric', 'define our metrics', 'metrics glossary', 'KPI framework', or 'which metrics should we build first'.
---

# Define the metric tree

This is Phase 4 of `new-analytics-onboarding`. The method lives in the orchestrator's shared
references so that the sequenced path and this standalone entry point can never drift.

**Read `../new-analytics-onboarding/references/phase-4-define-metric-tree.md` and follow it.**

Running the full engagement rather than this phase alone? Invoke `new-analytics-onboarding`
instead — it owns phase order, inputs, gates, and the engagement state file, and it reads this
same reference at Phase 4.
